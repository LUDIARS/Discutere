// @spec sentiment 空間の一本化
/** Backward-compatible import path for the shared 0+1 sentiment cascade. */
export {
  cascadeSentiment,
  type CascadeClients,
  type Polarity,
  type SentimentResult,
  type Signal,
} from "@ludiars/sentiment-core";
