node_modules
dist
data/*
!data/games/
!data/games/**
!data/discussions/
!data/discussions/**
!data/affects/
!data/affects/**
# 検索精度評価のゴールデンセット: 雛形のみ追跡 (実セットは KG 固有なのでローカル)。
!data/eval/
data/eval/*
!data/eval/retrieval-golden.example.json
.env
.env.secrets
.env.youtube.key
# サービス固有 config の実体 (secrets を含む)。example のみ追跡する。
discutere.config.json
*.tsbuildinfo
frontend/node_modules
frontend/dist
.tmp
*.stackdump
# FT モデルバイナリ (GGUF / HuggingFace weights) — Modelfile は追跡する
models/*.gguf
models/di-debate-v1/

# electron-builder の出力 (デスクトップアプリのパッケージ)
release/

# LLM 実行ログ / cost queue JSONL (src/persona-engine/llm が cwd/logs に書き出す)
logs/
